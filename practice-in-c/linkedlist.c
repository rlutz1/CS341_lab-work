#include <stdio.h>

/**
 * basic implementation of a linked list to 
 * include in the advent of code challenge 
 * for making a 2 dimensional matrix
 * 
 * basic definitions:
 * - header node -> next (node *), size
 * - a node -> next (node *)
 */