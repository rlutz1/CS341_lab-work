#include <stdio.h>

// way to run this in windows
//
// gcc ../../hello.c -o hello
// hello.exe
//
// notice that the windows version makes an exe file, and you just run it

int main() {
	printf("Hello World!");
	return 0;
}